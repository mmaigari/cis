<%= form_with(model: ccategory) do |form| %>
  <% if ccategory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ccategory.errors.count, "error") %> prohibited this ccategory from being saved:</h2>

      <ul>
        <% ccategory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
